方式1(用字典):
        var arr=[1,2,4,9,5,6,7,89,9,7,5];
        var obj={};
        for(var i=0;i<arr.length;i++){
            obj[arr[i]]=1;
        }
        console.log(obj);
        var result=[],i=0;
        for(result[i++] in obj);
        console.log(result);